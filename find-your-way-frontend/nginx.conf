server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    # Statische Assets direkt ausliefern
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot|mp4|webm|mp3|ogg|wav)$ {
        try_files $uri =404;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # React Router – alle Routen auf index.html
    location / {
        try_files $uri $uri/ /index.html =200;
    }
}
